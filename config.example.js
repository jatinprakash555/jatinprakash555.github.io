// --- CONFIGURATION TEMPLATE ---
// Create a copy named 'config.js' and fill in your actual API keys.
// 'config.js' is ignore by Git to prevent exposing secrets.
window.ENV_CONFIG = {
    supabaseUrl: "https://your-project.supabase.co",
    supabaseKey: "your-supabase-anon-key",
    hfToken: "hf_your-hugging-face-token"
};
